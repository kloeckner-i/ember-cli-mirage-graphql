query($firstName: String, $lastName: String) {
  people(firstName: $firstName, lastName: $lastName) {
    id
    address {
      line1
      line2
      city
      state
      zip
    }
    age
    firstName
    lastName
    pets {
      id
    }
  }
}
